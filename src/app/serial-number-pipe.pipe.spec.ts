import { SerialNumberPipePipe } from './serial-number-pipe.pipe';

describe('SerialNumberPipePipe', () => {
  it('create an instance', () => {
    const pipe = new SerialNumberPipePipe();
    expect(pipe).toBeTruthy();
  });
});
